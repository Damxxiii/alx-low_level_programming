#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer.
 * @n: the int to be checked
 * Return: the absolue value of int
 */

int main(void)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
